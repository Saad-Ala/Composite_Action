name: Hello world

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  Hello-word:
    runs-on: ubuntu-latest

    steps:
    - uses: colbyfayock/egghead-demo-github-action-hello-world@v1.0.1
    - uses: Saad-Ala/Node_CI@main
